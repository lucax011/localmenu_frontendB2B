version: '3.9'
services:
  web:
    build: .
    ports:
      - '8080:80'
    environment:
      - EXPO_PUBLIC_API_BASE_URL=${EXPO_PUBLIC_API_BASE_URL}
      - EXPO_PUBLIC_OCR_URL=${EXPO_PUBLIC_OCR_URL}
